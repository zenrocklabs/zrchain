// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: zrchain/zenbtc/params.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "zrchain.zenbtc";

/** Params defines the parameters for the module. */
export interface Params {
  depositKeyringAddr: string;
  stakerKeyID: number;
  ethMinterKeyID: number;
  unstakerKeyID: number;
  completerKeyID: number;
  rewardsDepositKeyID: number;
  changeAddressKeyIDs: number[];
  bitcoinProxyAddress: string;
  ethTokenAddr: string;
  controllerAddr: string;
  solana: Solana | undefined;
}

export interface Solana {
  signerKeyId: number;
  programId: string;
  nonceAccountKey: number;
  nonceAuthorityKey: number;
  mintAddress: string;
  feeWallet: string;
  fee: number;
  multisigKeyAddress: string;
  btl: number;
}

function createBaseParams(): Params {
  return {
    depositKeyringAddr: "",
    stakerKeyID: 0,
    ethMinterKeyID: 0,
    unstakerKeyID: 0,
    completerKeyID: 0,
    rewardsDepositKeyID: 0,
    changeAddressKeyIDs: [],
    bitcoinProxyAddress: "",
    ethTokenAddr: "",
    controllerAddr: "",
    solana: undefined,
  };
}

export const Params: MessageFns<Params> = {
  encode(message: Params, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.depositKeyringAddr !== "") {
      writer.uint32(10).string(message.depositKeyringAddr);
    }
    if (message.stakerKeyID !== 0) {
      writer.uint32(16).uint64(message.stakerKeyID);
    }
    if (message.ethMinterKeyID !== 0) {
      writer.uint32(24).uint64(message.ethMinterKeyID);
    }
    if (message.unstakerKeyID !== 0) {
      writer.uint32(32).uint64(message.unstakerKeyID);
    }
    if (message.completerKeyID !== 0) {
      writer.uint32(40).uint64(message.completerKeyID);
    }
    if (message.rewardsDepositKeyID !== 0) {
      writer.uint32(48).uint64(message.rewardsDepositKeyID);
    }
    writer.uint32(58).fork();
    for (const v of message.changeAddressKeyIDs) {
      writer.uint64(v);
    }
    writer.join();
    if (message.bitcoinProxyAddress !== "") {
      writer.uint32(66).string(message.bitcoinProxyAddress);
    }
    if (message.ethTokenAddr !== "") {
      writer.uint32(74).string(message.ethTokenAddr);
    }
    if (message.controllerAddr !== "") {
      writer.uint32(82).string(message.controllerAddr);
    }
    if (message.solana !== undefined) {
      Solana.encode(message.solana, writer.uint32(90).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Params {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.depositKeyringAddr = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stakerKeyID = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ethMinterKeyID = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.unstakerKeyID = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.completerKeyID = longToNumber(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.rewardsDepositKeyID = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag === 56) {
            message.changeAddressKeyIDs.push(longToNumber(reader.uint64()));

            continue;
          }

          if (tag === 58) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.changeAddressKeyIDs.push(longToNumber(reader.uint64()));
            }

            continue;
          }

          break;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.bitcoinProxyAddress = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.ethTokenAddr = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.controllerAddr = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.solana = Solana.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      depositKeyringAddr: isSet(object.depositKeyringAddr) ? globalThis.String(object.depositKeyringAddr) : "",
      stakerKeyID: isSet(object.stakerKeyID) ? globalThis.Number(object.stakerKeyID) : 0,
      ethMinterKeyID: isSet(object.ethMinterKeyID) ? globalThis.Number(object.ethMinterKeyID) : 0,
      unstakerKeyID: isSet(object.unstakerKeyID) ? globalThis.Number(object.unstakerKeyID) : 0,
      completerKeyID: isSet(object.completerKeyID) ? globalThis.Number(object.completerKeyID) : 0,
      rewardsDepositKeyID: isSet(object.rewardsDepositKeyID) ? globalThis.Number(object.rewardsDepositKeyID) : 0,
      changeAddressKeyIDs: globalThis.Array.isArray(object?.changeAddressKeyIDs)
        ? object.changeAddressKeyIDs.map((e: any) => globalThis.Number(e))
        : [],
      bitcoinProxyAddress: isSet(object.bitcoinProxyAddress) ? globalThis.String(object.bitcoinProxyAddress) : "",
      ethTokenAddr: isSet(object.ethTokenAddr) ? globalThis.String(object.ethTokenAddr) : "",
      controllerAddr: isSet(object.controllerAddr) ? globalThis.String(object.controllerAddr) : "",
      solana: isSet(object.solana) ? Solana.fromJSON(object.solana) : undefined,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.depositKeyringAddr !== "") {
      obj.depositKeyringAddr = message.depositKeyringAddr;
    }
    if (message.stakerKeyID !== 0) {
      obj.stakerKeyID = Math.round(message.stakerKeyID);
    }
    if (message.ethMinterKeyID !== 0) {
      obj.ethMinterKeyID = Math.round(message.ethMinterKeyID);
    }
    if (message.unstakerKeyID !== 0) {
      obj.unstakerKeyID = Math.round(message.unstakerKeyID);
    }
    if (message.completerKeyID !== 0) {
      obj.completerKeyID = Math.round(message.completerKeyID);
    }
    if (message.rewardsDepositKeyID !== 0) {
      obj.rewardsDepositKeyID = Math.round(message.rewardsDepositKeyID);
    }
    if (message.changeAddressKeyIDs?.length) {
      obj.changeAddressKeyIDs = message.changeAddressKeyIDs.map((e) => Math.round(e));
    }
    if (message.bitcoinProxyAddress !== "") {
      obj.bitcoinProxyAddress = message.bitcoinProxyAddress;
    }
    if (message.ethTokenAddr !== "") {
      obj.ethTokenAddr = message.ethTokenAddr;
    }
    if (message.controllerAddr !== "") {
      obj.controllerAddr = message.controllerAddr;
    }
    if (message.solana !== undefined) {
      obj.solana = Solana.toJSON(message.solana);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Params>, I>>(base?: I): Params {
    return Params.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Params>, I>>(object: I): Params {
    const message = createBaseParams();
    message.depositKeyringAddr = object.depositKeyringAddr ?? "";
    message.stakerKeyID = object.stakerKeyID ?? 0;
    message.ethMinterKeyID = object.ethMinterKeyID ?? 0;
    message.unstakerKeyID = object.unstakerKeyID ?? 0;
    message.completerKeyID = object.completerKeyID ?? 0;
    message.rewardsDepositKeyID = object.rewardsDepositKeyID ?? 0;
    message.changeAddressKeyIDs = object.changeAddressKeyIDs?.map((e) => e) || [];
    message.bitcoinProxyAddress = object.bitcoinProxyAddress ?? "";
    message.ethTokenAddr = object.ethTokenAddr ?? "";
    message.controllerAddr = object.controllerAddr ?? "";
    message.solana = (object.solana !== undefined && object.solana !== null)
      ? Solana.fromPartial(object.solana)
      : undefined;
    return message;
  },
};

function createBaseSolana(): Solana {
  return {
    signerKeyId: 0,
    programId: "",
    nonceAccountKey: 0,
    nonceAuthorityKey: 0,
    mintAddress: "",
    feeWallet: "",
    fee: 0,
    multisigKeyAddress: "",
    btl: 0,
  };
}

export const Solana: MessageFns<Solana> = {
  encode(message: Solana, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signerKeyId !== 0) {
      writer.uint32(8).uint64(message.signerKeyId);
    }
    if (message.programId !== "") {
      writer.uint32(18).string(message.programId);
    }
    if (message.nonceAccountKey !== 0) {
      writer.uint32(24).uint64(message.nonceAccountKey);
    }
    if (message.nonceAuthorityKey !== 0) {
      writer.uint32(32).uint64(message.nonceAuthorityKey);
    }
    if (message.mintAddress !== "") {
      writer.uint32(42).string(message.mintAddress);
    }
    if (message.feeWallet !== "") {
      writer.uint32(50).string(message.feeWallet);
    }
    if (message.fee !== 0) {
      writer.uint32(56).uint64(message.fee);
    }
    if (message.multisigKeyAddress !== "") {
      writer.uint32(66).string(message.multisigKeyAddress);
    }
    if (message.btl !== 0) {
      writer.uint32(72).int64(message.btl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Solana {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSolana();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.signerKeyId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.programId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.nonceAccountKey = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.nonceAuthorityKey = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.mintAddress = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.feeWallet = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.fee = longToNumber(reader.uint64());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.multisigKeyAddress = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.btl = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Solana {
    return {
      signerKeyId: isSet(object.signerKeyId) ? globalThis.Number(object.signerKeyId) : 0,
      programId: isSet(object.programId) ? globalThis.String(object.programId) : "",
      nonceAccountKey: isSet(object.nonceAccountKey) ? globalThis.Number(object.nonceAccountKey) : 0,
      nonceAuthorityKey: isSet(object.nonceAuthorityKey) ? globalThis.Number(object.nonceAuthorityKey) : 0,
      mintAddress: isSet(object.mintAddress) ? globalThis.String(object.mintAddress) : "",
      feeWallet: isSet(object.feeWallet) ? globalThis.String(object.feeWallet) : "",
      fee: isSet(object.fee) ? globalThis.Number(object.fee) : 0,
      multisigKeyAddress: isSet(object.multisigKeyAddress) ? globalThis.String(object.multisigKeyAddress) : "",
      btl: isSet(object.btl) ? globalThis.Number(object.btl) : 0,
    };
  },

  toJSON(message: Solana): unknown {
    const obj: any = {};
    if (message.signerKeyId !== 0) {
      obj.signerKeyId = Math.round(message.signerKeyId);
    }
    if (message.programId !== "") {
      obj.programId = message.programId;
    }
    if (message.nonceAccountKey !== 0) {
      obj.nonceAccountKey = Math.round(message.nonceAccountKey);
    }
    if (message.nonceAuthorityKey !== 0) {
      obj.nonceAuthorityKey = Math.round(message.nonceAuthorityKey);
    }
    if (message.mintAddress !== "") {
      obj.mintAddress = message.mintAddress;
    }
    if (message.feeWallet !== "") {
      obj.feeWallet = message.feeWallet;
    }
    if (message.fee !== 0) {
      obj.fee = Math.round(message.fee);
    }
    if (message.multisigKeyAddress !== "") {
      obj.multisigKeyAddress = message.multisigKeyAddress;
    }
    if (message.btl !== 0) {
      obj.btl = Math.round(message.btl);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Solana>, I>>(base?: I): Solana {
    return Solana.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Solana>, I>>(object: I): Solana {
    const message = createBaseSolana();
    message.signerKeyId = object.signerKeyId ?? 0;
    message.programId = object.programId ?? "";
    message.nonceAccountKey = object.nonceAccountKey ?? 0;
    message.nonceAuthorityKey = object.nonceAuthorityKey ?? 0;
    message.mintAddress = object.mintAddress ?? "";
    message.feeWallet = object.feeWallet ?? "";
    message.fee = object.fee ?? 0;
    message.multisigKeyAddress = object.multisigKeyAddress ?? "";
    message.btl = object.btl ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
