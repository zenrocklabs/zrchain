// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: zrchain/policy/additional_signature_passkey.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "zrchain.policy";

/** AdditionalSignaturePasskey is a message that contains passkey signature data */
export interface AdditionalSignaturePasskey {
  rawId: Uint8Array;
  authenticatorData: Uint8Array;
  clientDataJson: Uint8Array;
  signature: Uint8Array;
}

function createBaseAdditionalSignaturePasskey(): AdditionalSignaturePasskey {
  return {
    rawId: new Uint8Array(0),
    authenticatorData: new Uint8Array(0),
    clientDataJson: new Uint8Array(0),
    signature: new Uint8Array(0),
  };
}

export const AdditionalSignaturePasskey: MessageFns<AdditionalSignaturePasskey> = {
  encode(message: AdditionalSignaturePasskey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rawId.length !== 0) {
      writer.uint32(10).bytes(message.rawId);
    }
    if (message.authenticatorData.length !== 0) {
      writer.uint32(18).bytes(message.authenticatorData);
    }
    if (message.clientDataJson.length !== 0) {
      writer.uint32(26).bytes(message.clientDataJson);
    }
    if (message.signature.length !== 0) {
      writer.uint32(34).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AdditionalSignaturePasskey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAdditionalSignaturePasskey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rawId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.authenticatorData = reader.bytes();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.clientDataJson = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.signature = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AdditionalSignaturePasskey {
    return {
      rawId: isSet(object.rawId) ? bytesFromBase64(object.rawId) : new Uint8Array(0),
      authenticatorData: isSet(object.authenticatorData)
        ? bytesFromBase64(object.authenticatorData)
        : new Uint8Array(0),
      clientDataJson: isSet(object.clientDataJson) ? bytesFromBase64(object.clientDataJson) : new Uint8Array(0),
      signature: isSet(object.signature) ? bytesFromBase64(object.signature) : new Uint8Array(0),
    };
  },

  toJSON(message: AdditionalSignaturePasskey): unknown {
    const obj: any = {};
    if (message.rawId.length !== 0) {
      obj.rawId = base64FromBytes(message.rawId);
    }
    if (message.authenticatorData.length !== 0) {
      obj.authenticatorData = base64FromBytes(message.authenticatorData);
    }
    if (message.clientDataJson.length !== 0) {
      obj.clientDataJson = base64FromBytes(message.clientDataJson);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AdditionalSignaturePasskey>, I>>(base?: I): AdditionalSignaturePasskey {
    return AdditionalSignaturePasskey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AdditionalSignaturePasskey>, I>>(object: I): AdditionalSignaturePasskey {
    const message = createBaseAdditionalSignaturePasskey();
    message.rawId = object.rawId ?? new Uint8Array(0);
    message.authenticatorData = object.authenticatorData ?? new Uint8Array(0);
    message.clientDataJson = object.clientDataJson ?? new Uint8Array(0);
    message.signature = object.signature ?? new Uint8Array(0);
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
