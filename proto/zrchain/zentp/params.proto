syntax = "proto3";
package zrchain.zentp;

import "amino/amino.proto";
import "gogoproto/gogo.proto";

option go_package = "github.com/Zenrock-Foundation/zrchain/v5/x/zentp/types";

// Params defines the parameters for the module.
message Params {
  option (amino.name) = "zrchain/x/zentp/Params";
  option (gogoproto.equal) = true;

  uint64 zrchain_relayer_key_id = 1;

  Solana solana = 2;
}

message Solana {
  option (gogoproto.equal) = true;

  string program_id = 1;
  uint64 nonce_account_key = 2;
  uint64 nonce_authority_key = 3;
  string rpc_url = 4;
  string mint_address = 5;
  string fee_wallet = 6;
  uint64 fee = 7;
}